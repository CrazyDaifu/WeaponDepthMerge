# Build output

`WeaponDepthMerge.addon32` is the Release/Win32 binary produced from this repository.

The initial Battlefield 2 test reported successful weapon/arm depth merging with no bug observed. `SHA256SUMS.txt` is regenerated after the final offline rebuild.

Copy the `.addon32` file beside the 32-bit D3D9 ReShade DLL. See `../docs/TESTING.md` for required Generic Depth and MSAA settings.

