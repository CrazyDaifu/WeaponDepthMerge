# WeaponDepthMerge 1.1 RC11

Purpose: return to the smallest known baseline after RC10 remained unstable even with merging quarantined.

This build uses the RC1 implementation: version 1.0's native D3D9 merge path plus the API/null guards that prevent DXVK/Vulkan startup crashes. RC2-RC10 recovery and reload state logic is absent.

Status: diagnostic baseline; awaiting Battlefield 2 runtime confirmation.
