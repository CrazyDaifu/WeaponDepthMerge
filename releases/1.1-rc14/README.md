# WeaponDepthMerge 1.1 RC14

Purpose: determine whether ReShade add-on registration alone triggers the crash or whether an event callback is required.

This build only registers and unregisters the add-on. It has no event callbacks, overlay, resources, draw replay, or depth merging.

Status: diagnostic build; not a feature candidate.
